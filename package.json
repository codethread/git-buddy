{
  "name": "git-buddy",
  "version": "0.0.2",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "release": "bun run scripts/release.ts",
    "build": "bun build --entrypoints ./src/index.ts --outdir ./build --minify --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
